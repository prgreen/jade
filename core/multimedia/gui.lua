local M = {}

-- unique ids for each gui component
M.uid = 1

--- Create a text entry box with optional background color and border.
-- @param size Size of the text Entry, in characters
-- @param max_size Maximum size of entered text.
-- @param init_text Initial text in the box (defaults to "")
-- @return a table containing a new uid, a size (to know when gets focus)
-- a list of things to draw, a list of triggers to set/unset with focus  
function M.create_textEntry(size, max_size, init_text, text_color, background_color, border_color)
    

end






return M
